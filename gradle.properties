# Version of the kotlin-validator library this example builds against.
# Consumed in build.gradle.kts via `val kotlinValidatorVersion: String by project`.
kotlinValidatorVersion=0.1.0-beta.8

kotlin.code.style=official
kotlin.incremental=true
